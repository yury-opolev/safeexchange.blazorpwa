@page "/viewdata/{objectName}"

@using Microsoft.AspNetCore.Authorization

@attribute [Authorize]

<SafeExchange.Client.Web.Components.Pages.ViewData ObjectName=@this.ObjectName />

@code {
    [Parameter]
    public string ObjectName { get; set; }
}